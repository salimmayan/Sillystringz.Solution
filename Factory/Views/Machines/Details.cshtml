@{



  Layout = "_Layout";
}

@model Factory.Models.Machine

<h2>Machine Details</h2>
@* <div class="divClass">
  <span class="left"><span class="heavy">@Html.DisplayNameFor(model => model.MachineName):</span> @Html.DisplayFor(model
    => model.MachineName)</h3></span>
  <br><br>
  <span class="left"><span class="heavy">@Html.DisplayNameFor(model => model.MachineYear):</span> @Html.DisplayFor(model
    =>
    model.MachineYear)</h3></span>
</div>
<br> <br> <br> *@

<h4>Engineer the Machine belongs to:</h4>
<div class="divClass">
@foreach (var join in Model.JoinEntities)
{
  <p><span class="left olive2">@join.Machine.MachineName</span> <span
    class="right olive2">@join.Machine.MachineYear</span></p>
  @using (Html.BeginForm("DeleteMachine", "Machines"))
  {
    @Html.Hidden("joinId", @join.EngineerMachineId)
    <input type="submit" value="Delete" />
  }
}
</div>
<br> <br>

<p>@Html.ActionLink("Add a Machine", "AddMachine", new { id = Model.MachineId })</p>
<p>@Html.ActionLink("Back to Machine list", "Index")</p>
<p>@Html.ActionLink("Edit Machine", "Edit", new { id = Model.MachineId })</p>
<p>@Html.ActionLink("Delete Machine", "Delete", new { id = Model.MachineId })</p>