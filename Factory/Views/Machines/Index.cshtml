@{
  Layout = "_Layout";
}

@using Factory.Models;

<h1>Machines</h1>

@if (@Model.Count == 0)
{
  <h4>No Machines have been added yet!</h4>
}

@if (@Model.Count != 0)
{
  <div class="divClass">
    <p><span class="left olive2">Machine Name</span>   <span class="right olive2">Machine Year</span></p>
    <br>
    @foreach (Machine machine in Model)

    {
      <span class="left">@Html.ActionLink($"{machine.MachineName}", "Details", new { id = machine.MachineId })</span> <span
    class="right">
        @machine.MachineYear </span>
      <br>
    }
  </div>
}
<br>

<br>
<br>

<p>@Html.ActionLink("Add new machine", "Create")</p>